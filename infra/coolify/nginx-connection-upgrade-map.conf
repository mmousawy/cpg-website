# Must live in the http {} context (e.g. /etc/nginx/conf.d/).
# Empty Upgrade means a normal request — never send Connection: upgrade to Next.js.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
